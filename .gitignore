# Build artifacts
bin/
install/
checkpoints/
checkpoints_for_RTL/
logs/
*.tar.gz

# JSON configs (generated by uarch init) — but keep reference data
*.json
!data/reference.json

# Environment files — keep defaults
env/*.env
!env/default.env
!env/example.env

# Tool-specific
_gen_workspace/
.claude/
.humanize/
docs/superpowers/
